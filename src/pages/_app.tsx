/**
 * @fileoverview Custom App component for global configurations
 * @author NexSellPro
 * @created 2024-03-27
 * @lastModified 2024-03-27
 */

////////////////////////////////////////////////
// Imports:
////////////////////////////////////////////////
import '@/styles/globals.css';
import type { AppProps } from 'next/app';
import { ErrorBoundary } from 'react-error-boundary';

////////////////////////////////////////////////
// Types and Interfaces:
////////////////////////////////////////////////
// Using AppProps from next/app

////////////////////////////////////////////////
// Constants:
////////////////////////////////////////////////
// No constants needed for this page

////////////////////////////////////////////////
// Error Fallback:
////////////////////////////////////////////////
function ErrorFallback({ error }: { error: Error }) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-900 mb-4">
          Something went wrong
        </h1>
        <p className="text-gray-600">{error.message}</p>
      </div>
    </div>
  );
}

////////////////////////////////////////////////
// Page Component:
////////////////////////////////////////////////
function App({ Component, pageProps }: AppProps) {
  ////////////////////////////////////////////////
  // State and Hooks:
  ////////////////////////////////////////////////
  // No state or hooks needed for this page

  ////////////////////////////////////////////////
  // Data Fetching:
  ////////////////////////////////////////////////
  // No data fetching needed for this page
  // export async function getStaticProps() {}
  // export async function getServerSideProps() {}

  ////////////////////////////////////////////////
  // Event Handlers:
  ////////////////////////////////////////////////
  // No event handlers needed for this page

  ////////////////////////////////////////////////
  // Helper Functions:
  ////////////////////////////////////////////////
  // No helper functions needed for this page

  ////////////////////////////////////////////////
  // Render Methods:
  ////////////////////////////////////////////////
  return (
    <ErrorBoundary FallbackComponent={ErrorFallback}>
      <Component {...pageProps} />
    </ErrorBoundary>
  );
}

////////////////////////////////////////////////
// Export:
////////////////////////////////////////////////
export default App;
